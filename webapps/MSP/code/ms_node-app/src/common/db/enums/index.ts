/**
 * Client Type enum
 */
export enum ClientType {
    MySql = "mysql",
    MongoDb = "mongodb"
}

/**
 * Sort query options
 */
export enum SortOrder {
    Asc = "asc",
    Desc = "desc"
}

/**
 * Record Status enum
 */
export enum RecordStatus {
    Active = "active",
    Pending = "pending",
    Archived = "archived",
    Suspended = "suspended"
}
