/**
 * Route ids enum
 */
export enum Route {
    ping = "ping",
    meet = "meet",
    room = "room",
    user = "user"
}

/**
 * User routes
 */
export enum UserRoute {
    token = "token"
}

/**
 * User routes
 */
export enum RoomRoute {
    schedule = "schedule"
}
